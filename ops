{"author":{"id":"08468221db090fa9af39e58d6a9b2c05540151ac002b313cdfc0777721169c7d"},"ops":[{"type":1,"timestamp":1789641235,"nonce":"T3mLVOZhkcQjft+xCNTjGM+/1eI=","title":"Full-loop validation once the calibrated firmware is on the board and the laptop server is up","message":"(`cd web \u0026\u0026 pnpm build \u0026\u0026 pnpm start`).\n\nAcceptance checks\n- `curl localhost:3000/api/status` shows `online: true` for the real device (not the fake one).\n- Dashboard section cards, branches table and flow chart reflect real measured flow,\n  turbidity and TDS values.\n- A valve toggle in the web UI audibly clicks the physical relay and round-trips an ack + event.\n- The phone app (Expo Go) sees the same stream over the same WebSocket.\n- Leak demo: with a real leak on one branch the device closes that branch's valve on its own,\n  the dashboard shows the leak state and the event log records it, and `reset_leak` clears the latch.\n- `pnpm -C web smoke` prints SMOKE OK against the real device.\n\nDepends on the flash/calibrate issue.","files":null}]}